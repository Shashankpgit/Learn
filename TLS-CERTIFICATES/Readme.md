1. Here the most Important concept is look into the certificate expiry, the certificates shouldn't get expired.
2. The certificates should be renewed before expiry date to avoid any service disruption.
3. In-future we are going to use the multiple domain, look into that, how can we configure the in thatcase.
    Eg. Here for example look into `Obsrv` grafana, superset, keycloak, console.
4. Look into the Let's Encrypt service for generating and renewing the TLS certificates automatically.
