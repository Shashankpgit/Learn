apiVersion: v1
kind: Service
metadata:
  name: {{ include "myyapp.fullname" . }}
  labels:
    {{- include "myyapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "myyapp.selectorLabels" . | nindent 4 }}
